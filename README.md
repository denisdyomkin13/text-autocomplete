Нейросеть для автодополнения текстов

Учебный проект по созданию нейросети для автодополнения текстов на основе LSTM и Transformer моделей.

📋 Описание проекта
Проект реализует систему автодополнения текстов на основе двух подходов:

LSTM модель - рекуррентная нейронная сеть, обученная с нуля на датасете твитов

Transformer модель - предобученная модель distilgpt2 из библиотеки Transformers

🏗️ Структура проекта
text
text-autocomplete/
├── data/                            # Датасеты
│   ├── raw_dataset.csv              # "сырой" датасет sentiment140 (твиты)
│   ├── dataset_processed.csv        # очищенный и токенизированный датасет
│   ├── train.csv                    # тренировочная выборка
│   ├── val.csv                      # валидационная выборка
│   ├── test.csv                     # тестовая выборка
│   └── vocab.csv                    # словарь
│
├── src/                             # Исходный код
│   ├── data_utils.py                # Обработка датасета
│   ├── next_token_dataset.py        # Dataset для PyTorch
│   ├── lstm_model.py                # LSTM модель для автодополнения
│   ├── lstm_train.py                # Обучение LSTM модели
│   ├── eval_lstm.py                 # Оценка LSTM модели
│   └── eval_transformer_pipeline.py # Оценка Transformer модели
│
├── configs/                         # Конфигурационные файлы
│   └── config.yaml                  # Основной конфиг
│
├── models/                          # Сохраненные модели
│   ├── lstm_best_model.pth         # Лучшая LSTM модель
│   └── training_history.json       # История обучения
│
├── solution.ipynb                  # Основной ноутбук с решением
├── requirements.txt                # Зависимости проекта
└── README.md                       # Документация
📊 Используемые данные
Проект использует датасет sentiment140 - 1.6 миллиона твитов с эмоциональной окраской. Каждый твит предварительно обрабатывается:

Удаление упоминаний (@username)

Удаление URL и хэштегов

Приведение к нижнему регистру

Токенизация с помощью NLTK

🚀 Установка и запуск
1. Клонирование репозитория
bash
git clone <repository-url>
cd text-autocomplete
2. Установка зависимостей
bash
pip install -r requirements.txt
3. Подготовка данных
Поместите ваш датасет в data/raw_dataset.csv или используйте тестовый датасет:

bash
# Создание тестового датасета
python -c "
sample_tweets = ['I love this movie', 'Going to the beach', 'Happy birthday']
import pandas as pd; pd.DataFrame({'text': sample_tweets}).to_csv('data/raw_dataset.csv', index=False)
"
4. Запуск полного пайплайна
bash
# Вариант 1: Через мастер-скрипт (Linux/Mac)
chmod +x master_pipeline.sh
./master_pipeline.sh

# Вариант 2: Через Python скрипт
python run_complete_project.py

# Вариант 3: Поэтапно
python src/data_utils.py                    # Предобработка данных
python src/lstm_train.py                    # Обучение LSTM модели
python -m src.eval_transformer_pipeline     # Оценка Transformer
5. Анализ результатов
Откройте Jupyter ноутбук для анализа:

bash
jupyter notebook solution.ipynb
🧠 Модели
LSTM модель
Архитектура: Многослойная LSTM с механизмом внимания

Embedding размер: 256

Hidden размер: 512

Слои: 2

Dropout: 0.3

Размер словаря: ~10,000 слов

Transformer модель
Модель: distilgpt2 (упрощенная версия GPT-2)

Параметры: 82 миллионов

Предобучение: На английских текстах

Генерация: Сэмплирование с температурой 0.9

📈 Метрики оценки
Для оценки качества используется метрика ROUGE:

ROUGE-1: Совпадение униграмм (отдельных слов)

ROUGE-2: Совпадение биграмм (пар слов)

ROUGE-L: Наибольшая общая подпоследовательность

📊 Результаты
Примерные метрики на тестовом наборе:
Модель	ROUGE-1	ROUGE-2	ROUGE-L	Размер	Время генерации
LSTM	0.25-0.35	0.10-0.20	0.22-0.30	~10MB	~0.1 сек
distilgpt2	0.35-0.45	0.18-0.28	0.32-0.40	~300MB	~0.5 сек
Примеры генерации:
Промпт: "I love this movie because"

LSTM: "i love this movie because it is so funny"

Transformer: "i love this movie because it reminds me of my childhood"

Промпт: "Going to the beach"

LSTM: "going to the beach today with my friends"

Transformer: "going to the beach is my favorite summer activity"

🎯 Использование обученных моделей
Генерация с помощью LSTM
python
from src.lstm_model import LSTMAutocomplete, TextGenerator
import torch
import pandas as pd

# Загрузка словаря
vocab_df = pd.read_csv('data/vocab.csv')
word2idx = dict(zip(vocab_df['word'], vocab_df['index']))
idx2word = {v: k for k, v in word2idx.items()}

# Загрузка модели
model = LSTMAutocomplete(vocab_size=len(word2idx), embedding_dim=256, hidden_dim=512)
model.load_state_dict(torch.load('models/lstm_best_model.pth'))

# Генерация
generator = TextGenerator(model, idx2word)
result = generator.generate_text("I love", max_length=20, temperature=0.9)
print(result)
Генерация с помощью Transformer
python
from src.eval_transformer_pipeline import TransformerEvaluator

evaluator = TransformerEvaluator(model_name="distilgpt2")
result = evaluator.generate_continuation("I love", max_new_tokens=20)
print(result)
🏆 Рекомендации
Когда использовать LSTM:
✅ Ограниченные ресурсы (CPU, мобильные устройства)

✅ Маленький размер модели (~10MB)

✅ Быстрая генерация

✅ Достаточно для простых дополнений

Когда использовать Transformer:
✅ Высокое качество генерации важно

✅ Достаточно памяти и вычислительных ресурсов

✅ Нужно разнообразие в генерации

✅ Поддержка сложных контекстов

🔧 Настройка параметров
Все параметры настраиваются в файлах конфигурации:

Предобработка (configs/config.yaml):

yaml
preprocessing:
  max_vocab_size: 10000
  max_sequence_length: 50
  sample_size: 50000
Обучение LSTM:

yaml
lstm_model:
  embedding_dim: 256
  hidden_dim: 512
  num_layers: 2
  dropout: 0.3
  learning_rate: 0.001
Генерация:

yaml
generation:
  temperature: 0.9
  top_k: 50
  top_p: 0.95
  max_length: 20
📝 Требования к системе
Python: 3.8+

Память: Минимум 4GB RAM

Диск: 2GB свободного места

GPU: Рекомендуется для ускорения обучения (но не обязательно)

Для обучения на полном датасете sentiment140:

CPU: 8GB RAM, обучение за 2-5 часов

GPU: 4GB VRAM, обучение за 30-60 минут

🐛 Решение проблем
1. Проблема с импортом модулей
python
# Добавьте в начало скрипта:
import sys
import os
sys.path.append(os.path.join(os.getcwd(), 'src'))
2. Недостаточно памяти
Уменьшите batch_size в конфигурации

Используйте sample_size для ограничения данных

Уменьшите embedding_dim и hidden_dim

3. Долгое обучение
Уменьшите num_epochs

Используйте GPU если доступно

Уменьшите размер датасета

4. Проблемы с качеством генерации
Увеличьте размер обучающей выборки

Настройте параметры генерации (temperature, top_k)

Увеличьте размер модели

📚 Используемые технологии
PyTorch: Для создания и обучения нейронных сетей

Transformers: Для работы с предобученными моделями

NLTK: Для токенизации текста

Pandas & NumPy: Для обработки данных

Scikit-learn: Для разделения данных

Matplotlib & Seaborn: Для визуализации результатов

📄 Лицензия
Этот проект создан в учебных целях. Датасет sentiment140 доступен для исследовательского использования.

👥 Авторы
Учебный проект в рамках курса по нейронным сетям

Разработано с использованием материалов от DeepSeek

🤝 Вклад в проект
Если вы хотите внести вклад в проект:

Форкните репозиторий

Создайте ветку для вашей функции

Внесите изменения

Создайте Pull Request

📞 Контакты
Для вопросов и предложений создавайте Issues в репозитории проекта.